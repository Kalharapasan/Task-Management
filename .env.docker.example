# Copy to .env in the project root before running `docker compose up`.
# docker-compose.yml reads these to configure MySQL/backend secrets.

MYSQL_ROOT_PASSWORD=rootpassword
JWT_SECRET=replace_this_with_a_long_random_secret_string
JWT_REFRESH_SECRET=replace_this_with_a_different_long_random_secret_string
